# replace with user accessing the files
gid = nobody
uid = nobody

[sponsorblock]
use chroot = no
max connections = 10
# path to mirrored files
path = /mirror
comment = sponsorblock-database
read only = true
refuse options = delete zl
# disallow deleting files clientside